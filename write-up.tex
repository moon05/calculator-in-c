\documentclass[12pt]{article}
\usepackage{times}
\usepackage[T1]{fontenc}

\title{\bf{Description of Calculator Project in C}}
\author{Abdullah Al Mamun}
\date{October 11th, 2015}

\begin{document}
\maketitle

\large{

My program can calculate the results of many expressions. It consists of a Scanner, Parser and some other necessary functions.


\textbf{Parser}
My Parser analyzes the the tokenized values from the scanner.\\
My CFG:\\
\\
$Program \rightarrow Statement_List$\\
$Statement_List \rightarrow Statement | \epsilon\\$
$Statement \rightarrow id: Expression | read id | write expr\\$
$Expression \rightarrow Term Term_tail$
$Term_tail \rightarrow + Term Term_tail | \epsilon\\$
$Term_tail \rightarrow - Term Term_tail| \epsilon\\$
$Factor_tail \rightarrow * Factor Factor_tail | \epsilon\\$
$Factor_tail \rightarrow / Factor Factor_tail | \epsilon\\$
$Factor_tail \rightarrow \% Factor Factor_tail | \epsilon\\$
$Factor \rightarrow ( Expression ) | id | number$\\


\textbf{First}\\
Program(id,read,write,$$)\\
Statement_List(id,read,write)\\
Statement(id,read,write)\\
expr((,id,write)\\
term_tail(+,-)\\
term((,id,number)\\
factor_tail(*,/,\%)\\
factor((,id,number)\\
\\
\textbf{Follow}
\\
id \textbf{{}+, -, *, /, ), :=, id, read, write, $$\textbf{}}\\
number \textbf{}}+, -, *, /, ), id, read, write, $$\textbf{}}\\
read \textbf{}}id\textbf{}}\\
write \textbf{}}(, id, number\textbf{}}
( {(, id, number}
) {+, -, *, /, ), id, read, write, $$} := {(, id, number}
+ {(, id, number}
- {(, id, number}
* {(, id, number}
/ {(, id, number}
$$ ∅
program ∅
Statement_List {$$}
stmt {id, read, write, $$}




}
\end{document}